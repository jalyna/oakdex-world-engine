import { CharState, MapData } from '.';
export declare function isFieldWalkable(mapData: MapData, chars: CharState[], charId: string, x: number, y: number): boolean;
export default function (mapData: MapData, chars: CharState[], charId: string): boolean;
